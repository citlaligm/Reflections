What happens when you initialize a repository? Why do you need to do it?
A hidden directory for git is createad to be able to make commits

How is the staging area different from the working directory and the repository?
What value do you think it offers?
Makes you add all the files that belong to the same logical change in order to prepare them to commit.

How can you use the staging area to make sure you have one commit per logical
change?
You add only the files that have something in common, so that the message in the commit could be useful and clear.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
When I want to experiment with a feature or implementation. It keeps good quality of the master branch 

How do the diagrams help you visualize the branch structure?
It helps you to be sure where are you before performing any other action that can be harmful to the proeject.


What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?
It joins all the changes from the master branch and any other branch. 

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?
Automatic merges are very efficients when there are no conflicts between lines. 